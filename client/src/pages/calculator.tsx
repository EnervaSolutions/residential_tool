import { WindowReplacementCalculator } from "@/components/window-replacement-calculator";

export default function Calculator() {
  return (
    <div className="min-h-screen bg-background">
      <WindowReplacementCalculator />
    </div>
  );
}
