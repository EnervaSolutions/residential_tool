import { DoorReplacementCalculator } from "@/components/door-replacement-calculator";

export default function DoorCalculator() {
  return (
    <div className="min-h-screen bg-background">
      <DoorReplacementCalculator />
    </div>
  );
}